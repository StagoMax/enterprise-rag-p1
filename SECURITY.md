# 安全策略

## 支持范围

仓库当前处于研究预览阶段，不应直接承载生产凭据、真实企业私有数据或面向公网的未加固服务。安全修复以默认分支 `main` 为准，历史实验配置不单独维护。

## 私下报告漏洞

请使用仓库的 [GitHub Private Vulnerability Reporting](https://github.com/StagoMax/enterprise-rag-p1/security/advisories/new) 提交报告，不要公开创建 Issue。

报告中请包含：

- 受影响的提交、模块和配置；
- 可复现步骤或最小验证代码；
- 对 ACL、跨租户数据、凭据、SQL 工具或生成链路的影响；
- 已知的缓解方式（如有）。

维护者会先确认影响范围，再协调修复与披露时间。请勿在获得修复确认前公开利用细节。

## 运行者责任

- 替换默认开发 JWT secret，并关闭不需要的开发端点。
- 把模型密钥保存在未跟踪的 `.env` 或专用密钥管理系统中。
- 仅授予 SQL 工具只读账号，并在数据库层继续限制可访问对象。
- 使用真实企业数据前独立审查数据、模型和依赖许可证。
- 将公开 TechQA 实验结果视为研究证据，而不是生产安全认证。
